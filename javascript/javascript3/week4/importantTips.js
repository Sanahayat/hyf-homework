/* selectOptiosForQuestion.addEventListener('change', () => {
                const indexOfSelectedOption = selectOptiosForQuestion.selectedIndex;

                const selectedOption = question.options[indexOfSelectedOption];
                console.log(selectedOption);
                
                const selectedOptionHTMLTagsArray = document.querySelectorAll('option');
                console.log(selectedOptionHTMLTagsArray[indexOfSelectedOption])

                const selectedOptionHTMLTag =selectedOptionHTMLTagsArray[indexOfSelectedOption];
                //console.log(selectedOptionHTMLTag.getAttribute('data-is-answer'))
                if(selectedOptionHTMLTag.getAttribute('data-is-answer') == 'true' )
                {
                    console.log('its correct')
                    totalScore ++;
                }else
                    console.log('false')
                
            })
            console.log(totalScore) */
              //console.log(QuizUl)