//Converting X hours and Y minutes into minutes:
let xHours = 4;
let yMin = 30;
let convertHourstoMin = xHours * 60;
let finalMin = yMin + convertHourstoMin;
console.log(xHours+ " hours " + yMin + " minutes to minutes are: " + finalMin);